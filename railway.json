{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "healthcheckPath": "/api/siteping",
    "healthcheckTimeout": 300,
    "preDeployCommand": [
      "cd apps/demo && bunx prisma db push --accept-data-loss"
    ]
  }
}
